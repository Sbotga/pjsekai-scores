image: python:latest

before_script:
  - python --version
  - pip --version

deploy:
  stage: deploy
  script:
    - pip install --upgrade build twine
    - python -m build
    - echo $PYPI_PASSWORD
    - python -m twine upload dist/* --username __token__ --password $PYPI_PASSWORD
  only:
    - tags
